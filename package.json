{
  "name": "dlavie-workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "packageManager": "pnpm@10.24.0",
  "scripts": {
    "dev": "pnpm -r --parallel --filter ./apps/** --if-present run dev",
    "build": "pnpm run typecheck && pnpm -r --filter=./artifacts/** --filter=./apps/** --filter=./packages/** --filter=./scripts --filter=!@workspace/dlavie-mobile --if-present run build",
    "lint": "pnpm -r --if-present run lint",
    "test": "pnpm -r --if-present run test",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter ./artifacts/** --filter ./apps/** --filter ./packages/** --filter ./scripts --if-present run typecheck"
  },
  "devDependencies": {
    "prettier": "^3.8.3",
    "turbo": "^2.6.1",
    "typescript": "~5.9.3"
  }
}
